applications:
  - name: dev-quizme-backend
    memory: 1G
    path: build/libs/quiz-backend-all.jar
    env:
      SPRING_PROFILES_ACTIVE: cloud
      SPRING_JPA_PROPERTIES_HIBERNATE_CONNECTION_PROVIDER_DISABLES_AUTOCOMMIT: false
